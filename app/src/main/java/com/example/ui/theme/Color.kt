package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme West Bengal Anganwadi / ASHA Colors
val LightPrimary = Color(0xFFFF6F00)      // Anganwadi Saffron / Orange (Primary CTA & Energy)
val LightSecondary = Color(0xFF4CAF50)    // Leaf Green (Growth & Healthy Metrics)
val LightTertiary = Color(0xFFFFC107)     // Warning Amber (Moderate Acute Malnutrition / At-Risk)
val LightBackground = Color(0xFFF9F9F9)   // Pure Off-White (Eye Strain minimization)
val LightSurface = Color(0xFFFFFFFF)      // Pure card surface for Bento Grid layout
val LightOnPrimary = Color(0xFFFFFFFF)
val LightOnSecondary = Color(0xFFFFFFFF)
val LightOnBackground = Color(0xFF211B1A)
val LightOnSurface = Color(0xFF211B1A)

// Dark Theme Variants (Optimized contrast)
val DarkPrimary = Color(0xFFFFB74D)       // Pastel Saffron
val DarkSecondary = Color(0xFF81C784)     // Soft Leaf Green
val DarkTertiary = Color(0xFFFFD54F)      // Pastel Amber
val DarkBackground = Color(0xFF1F120A)    // Warm deep charcoal wood/earth canvas
val DarkSurface = Color(0xFF2A1D15)       // Warm surface cards
val DarkOnPrimary = Color(0xFF1F120A)
val DarkOnSecondary = Color(0xFF1F120A)
val DarkOnBackground = Color(0xFFFEF7F3)
val DarkOnSurface = Color(0xFFFEF7F3)

// Alert Semantics (Always high accessibility colors)
val ColorHighRisk = Color(0xFFD32F2F)     // Critical cases / Severe Malnutrition (SAM) red
val ColorWarning = Color(0xFFFFC107)      // Moderate Malnutrition amber
val ColorNormal = Color(0xFF4CAF50)       // Normal / Healthy green
val ColorInfo = Color(0xFF1976D2)         // Information blue

